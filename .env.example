# Anthropic API key — get one from https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: where the local wiki-raw inbox lives
WIKI_RAW_PATH=~/wiki-raw

# Optional: path to the company-wiki content repo (cloned separately)
WIKI_CONTENT_REPO=~/repos/company-wiki
