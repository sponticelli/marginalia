"""Engineering rationale records (timestamped receipts, not wiki content).

Lives in the engine repo, not the wiki: model-selection rationale,
performance receipts, and architectural decisions belong with the code
that depends on them. The wiki repo gets `_ops/schemas/`; this gets the
"why" behind the engine's defaults.
"""
